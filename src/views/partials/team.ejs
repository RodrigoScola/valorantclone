<% for (let i =0 ; i < team.length; i++) { %>
<div data-agent-id="<%= team[i].id %>" class="p-4 rounded">
  <p><%= team[i].name %></p>
  <img src="<%= team[i].images.icon.url %>" alt="" />
</div>

<% } %>